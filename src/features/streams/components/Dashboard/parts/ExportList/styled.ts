import styled from 'styled-components'

import { breakpoints } from '@/styles/breakpoints'

export const StyledExportList = styled.section`
  margin-bottom: 5rem;
  width: 100%;
  max-width: ${breakpoints.content.large};
`
